server.port=8090

### MySQL

#spring.datasource.url = jdbc:mysql://mysql_server:3306/springdockertestdb
# both work - service name & container name
#spring.datasource.url = jdbc:mysql://mysql-db:3306/springdockertestdb

spring.datasource.url = jdbc:mysql://localhost:3306/springdockertestdb

spring.datasource.username = myuser
spring.datasource.password = mypass123
#spring.datasource.driver-class = com.mysql.jdbc.Driver

# Keep the connection alive if idle for a long time (needed in production)
#spring.datasource.testWhileIdle = true
#spring.datasource.validationQuery = SELECT 1

# Show or not log for each sql query
spring.jpa.show-sql = true

# Hibernate ddl auto (create, create-drop, update)
spring.jpa.hibernate.ddl-auto = create

# Naming strategy
spring.jpa.hibernate.naming-strategy = org.hibernate.cfg.ImprovedNamingStrategy

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

### MongoDB
mongo.uri = mongodb://localhost:27017
mongo.db.name = testdb
